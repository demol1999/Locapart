# Configuration pour les TESTS
JWT_SECRET_KEY=test-secret-key-for-automated-testing-only

# Base de données de test (utilise SQLite pour les tests)
DATABASE_URL=sqlite:///./test_locappart.db

# Configuration JWT pour les tests (expiration courte)
ACCESS_TOKEN_EXPIRE_MINUTES=5

# Environnement
ENVIRONMENT=test

# Désactive les logs en mode test
LOG_LEVEL=ERROR