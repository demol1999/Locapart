# Configuration de sécurité PRODUCTION
# GÉNERER UNE NOUVELLE CLÉ UNIQUE POUR LA PRODUCTION !
JWT_SECRET_KEY=your-super-secret-production-jwt-key-minimum-32-characters

# Configuration de la base de données PRODUCTION
# Utiliser des credentials sécurisés pour la production
DATABASE_URL=mysql+pymysql://prod_user:secure_password@prod_host/locappart_prod_db

# Configuration JWT
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environnement
ENVIRONMENT=production

# Configuration HTTPS (pour la production)
# Assure-toi que ton serveur web (nginx/apache) gère HTTPS
FORCE_HTTPS=true

# Configuration CORS (ajuster selon tes domaines de production)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com